```shell
sudo init 3 #关闭桌面
sudo systemctl set-default multi-user.target
sudo reboot
```

```shell
sudo apt update 
sudo apt upgrade -y
sudo apt autoremove -y
sudo apt install cuda-toolkit-10-2 -y
sudo apt install nvidia-cudnn8 -y
sudo apt install python3-pip -y
sudo pip3 install -U jetson-stats
sudo apt install -y apt-transport-https curl
sudo apt install nvidia-jetpack -y
sudo apt autoremove -y
sudo apt clean
sudo systemctl set-default multi-user.target
sudo reboot
```

```shell
sudo apt install nvidia-cudnn8 -y && sudo apt install python3-pip -y && sudo pip3 install -U jetson-stats && sudo apt install -y apt-transport-https curl && sudo apt clean
```

```shell
echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://mirrors.ustc.edu.cn/kubernetes/core:/stable:/v1.28/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list
cd /etc/apt
sudo mkdir keyrings
cd
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.28/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
```