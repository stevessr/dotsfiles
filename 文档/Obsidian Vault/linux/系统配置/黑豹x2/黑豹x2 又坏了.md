# init
```shell
sudo apt update
sudo apt upgrade
```

# 先开个代理
```shell
export proxy="http://172.22.29.228:7897"
export http_proxy="http://172.22.29.228:7897"
export https_proxy="http://172.22.29.228:7897"
```

```shell
export proxy=""
export http_proxy=""
export https_proxy=""
```

# [安装面板 | 耗子面板](https://ratpanel.github.io/quickstart/install.html) 
```
curl -fsLm 10 -o install.sh https://dl.cdn.haozi.net/panel/install.sh && sudo bash install.sh
```
# nodejs启动
```shell
curl -o- https://fnm.vercel.app/install | bash
fnm install 22
node -v
corepack enable pnpm
pnpm -v
```

# uv启动
```shell
curl -LsSf https://astral.sh/uv/install.sh | sh
```

# rust启动
```shell
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

# owu 启动(✧∀✧)
```shell
uv venv -p 3.12
sudo apt install python3.13-full
sudo apt install python3-pip
sudo apt install pipx
```

```conf
stevessr@panther-x2 /m/disk> cat   /etc/systemd/system/open-webui.service
[Unit]
Description=Open WebUI Application Service
# Ensures network is up, the disk is mounted, and potentially db/redis are started before this service
After=network.target mnt-disk.mount postgresql.service redis.service
Wants=mnt-disk.mount postgresql.service redis.service # Less strict dependency if needed

[Service]
Type=simple
# === 用户和组 ===
# !! 修改为你实际运行此服务的用户和组 !!
# 确保此用户对 WorkingDirectory 和 ExecStart 中的路径有权限
User=stevessr
#Group=stevessr # 通常与用户相同

# === 工作目录 ===
WorkingDirectory=/mnt/disk/opwebui

# === 环境变量 ===
Environment="ENV=prod"
Environment="WEBUI_NAME=steve's webui"
Environment="CUSTOM_NAME=steve's webui"
Environment="WEBUI_URL=https://any.aaca.eu.org"
Environment="PORT=8088"
Environment="ENABLE_CHANNELS=TRUE"
Environment="DEFAULT_LOCALE=zh_cn"
Environment="AIOHTTP_CLIENT_TIMEOUT_OPENAI_MODEL_LIST=10"
Environment="DATA_DIR=/mnt/disk/opwebui"
Environment="DATABASE_URL=postgresql://opwebui:opwebui@localhost/opwebui"
Environment="ENABLE_WEBSOCKET_SUPPORT=TRUE"
Environment="WEBSOCKET_MANAGER=REDIS"
Environment="WEBSOCKET_REDIS_URL=redis://localhost:6379/0"
Environment="REDIS_URL=redis://localhost:6379/0"
Environment="HF_ENDPOINT=https://hf-mirror.com"
Environment="proxy="
Environment="http_proxy="
Environment="https_proxy="
# 建议为 Python 应用添加，确保日志不被缓冲，能更快出现在 journal 中
Environment="PYTHONUNBUFFERED=1"

# === 执行命令 ===
# 完整的 Python 解释器路径 + open-webui 脚本路径 + 参数
ExecStart=/mnt/disk/opwebui/.venv/bin/python3 /mnt/disk/opwebui/.venv/bin/open-webui serve --port 8088

# === 重启策略 ===
Restart=on-failure # 或者使用 always
RestartSec=5       # 重启前等待 5 秒

# === 资源限制 ===
MemoryMax=2G

# === 日志记录 ===
# 将标准输出和标准错误重定向到 systemd journal
StandardOutput=journal
StandardError=journal

# === 其他 ===
# 如果服务需要绑定到特权端口 (<1024)，可能需要添加:
# AmbientCapabilities=CAP_NET_BIND_SERVICE
# 或者使用 setcap 'cap_net_bind_service=+ep' /path/to/python3

[Install]
WantedBy=multi-user.target
```

# [AstrBot: ✨ ](https://github.com/AstrBotDevs/AstrBot) 启动
```shell
git clone https://github.com/AstrBotDevs/AstrBot.git
cd AstrBot
uv sync
```

```shell
sudo nano /etc/systemd/system/astrabot.service 
```

```shell
[Unit]
Description=AstrBot Application Service
# 确保磁盘挂载和网络可用后启动
# 如果 AstrBot 依赖其他服务 (如数据库、Redis)，在这里添加 Requires= 和 After=
After=network.target mnt-disk.mount
Wants=mnt-disk.mount

[Service]
Type=simple
# === 用户和组 ===
# !! 确认这是正确的用户，并且该用户对 WorkingDirectory 和脚本有权限 !!
User=stevessr
# Group=stevessr # 可以省略，Systemd 会使用用户的主组。如果遇到权限问题或需要特定组，请取消注释并确保组存在。

# === 工作目录 ===
# 程序将从此目录执行，这对于加载相对路径的文件很重要
WorkingDirectory=/mnt/disk/AstrBot

# === 执行命令 ===
# !! 关键：确认 '/mnt/disk/AstrBot/.venv/bin/python' 是你虚拟环境中 Python 的正确绝对路径 !!
ExecStart=/mnt/disk/AstrBot/.venv/bin/python main.py

# === 环境变量 ===
# 可选：添加你的应用可能需要的环境变量
# Environment="SOME_VARIABLE=some_value"
# 推荐：确保 Python 日志不缓冲，直接输出到 journald
Environment="PYTHONUNBUFFERED=1"
Environment="proxy="
Environment="http_proxy="
Environment="https_proxy="

# === 重启策略 ===
# 如果服务失败则重启
Restart=on-failure
# 重启前等待 5 秒
RestartSec=5

# === 日志记录 ===
# 将标准输出和错误重定向到 systemd journal
StandardOutput=journal
StandardError=journal

[Install]
# 使服务可以在多用户模式下启用 (开机自启)
WantedBy=multi-user.target
```

# 安装bottom
```shell
sudo apt install bottom
```

# [SillyTavern](https://github.com/SillyTavern/SillyTavern) 启动
```shell
git clone https://github.com/SillyTavern/SillyTavern.git
cd SillyTavern
pnpm i
```

```conf
[Unit]
Description=SillyTavern Web UI
# Start after the network stack is up
After=network.target mnt-disk.mount
Wants=mnt-disk.mount

[Service]
# Type=simple is usually appropriate for Node.js servers that run in the foreground
Type=simple

# User and Group to run the service as
User=stevessr
#Group=stevessr # Or the primary group of stevessr if different

# Working directory where server.js and the data directory reside
WorkingDirectory=/mnt/disk/SillyTavern

# The command to start the service
# !!! WARNING: This path might be temporary! Replace with persistent fnm path if possible !!!
ExecStart=/run/user/1000/fnm_multishells/18712_1745111471136/bin/node server.js

# Restart the service if it fails
Restart=on-failure
RestartSec=5

# Optional: Set environment variables if needed
# Environment="NODE_ENV=production"

# Standard output and error logging will go to the systemd journal
StandardOutput=journal
StandardError=journal

[Install]
# Enable the service for the default multi-user target
WantedBy=multi-user.target
```

# [new-api:](https://github.com/QuantumNous/new-api) 启动
```shell
wget https://github.com/QuantumNous/new-api/releases/latest/download/one-api-arm64
```

```shell
sudo nano /etc/systemd/system/new-api.service
```

```conf
[Unit]
Description=New API Service (one-api-arm64)
# Start after network is up and database/cache services are running
# Make sure 'redis-server.service' and 'postgresql.service' are the correct names for your system
# Use 'Wants' instead of 'Requires' so this service still tries to start if they fail
After=network.target redis.service postgresql.service mnt-disk.mount
Wants=redis.service postgresql.service mnt-disk.mount

[Service]
# The process doesn't fork
Type=simple

# User and Group to run the service as
User=stevessr
#Group=stevessr # Or the primary group of stevessr if different

# Working directory where the executable resides and relative paths (like TIKTOKEN_CACHE_DIR) are based
WorkingDirectory=/mnt/disk/development/new-api

# Environment variables needed by the application
Environment="STREAMING_TIMEOUT=6000"
Environment="REDIS_CONN_STRING=redis://localhost:6379/1"
Environment="SQL_DSN=postgres://newapi:newapi@localhost/newapi"
Environment="MEMORY_CACHE_ENABLED=true"
Environment="PORT=8081"
# Note: The path below is relative to the WorkingDirectory
Environment="TIKTOKEN_CACHE_DIR=./cache/tiktoken"

# The command to start the service
ExecStart=/mnt/disk/development/new-api/one-api-arm64

# Restart the service if it exits unexpectedly
Restart=on-failure
RestartSec=5s # Wait 5 seconds before restarting

# Optional: Set resource limits if needed
# LimitNOFILE=65536

# Send standard output and error to the systemd journal
StandardOutput=journal
StandardError=journal
# Note: This replaces the file logging defined in new.config.js.
# Use 'journalctl -u new-api.service' to view logs.

[Install]
# Enable the service for the default multi-user target (standard system boot)
WantedBy=multi-user.target
```

# [deepseek2api:](https://github.com/iidamie/deepseek2api) 启动
```shell
git clone https://github.com/iidamie/deepseek2api.git
cd deepseek2api
uv venv
source .venv/bin/activate.fish
uv pip install -r requirements.txt
```

```shell
sudo nano /etc/systemd/system/deepseekapi.service
```

```conf
[Unit]
Description=Deepseek API Service (Uvicorn)
# Start after the network stack is available
After=network.target mnt-disk.mount
Wants=mnt-disk.mount

[Service]
# Type=simple is usually appropriate for uvicorn/gunicorn
Type=simple

# User and Group to run the service as
User=stevessr
#Group=stevessr # Or the primary group of stevessr if different

# The directory where your app.py (or app folder) resides
WorkingDirectory=/mnt/disk/development/deepseek2api

# The command to start the service using the virtualenv's uvicorn
# Ensure this full path to uvicorn is correct and accessible
ExecStart=/mnt/disk/development/deepseek2api/.venv/bin/uvicorn app:app --host 0.0.0.0 --port 5001

# Restart the service if it fails unexpectedly
Restart=on-failure
RestartSec=5

# Optional: Set environment variables if your application needs them
# Environment="MY_API_KEY=your_key_here"
# Environment="DATABASE_URL=your_db_connection_string"

# Configure logging to systemd journal
StandardOutput=journal
StandardError=journal

[Install]
# Enable the service for the default multi-user target (standard system boot)
WantedBy=multi-user.target
```

# [mcpo](https://github.com/open-webui/mcpo)
```shell
git clone https://github.com/open-webui/mcpo.git
cd mcpo
uv syncg
uv pip install .
```

```shell
sudo nano /etc/systemd/system/mcpo.service
```

```conf
[Unit]
Description=MCPO Application Service
# Start after the network stack is generally up
After=network.target mnt-disk.mount
Wants=mnt-disk.mount
# If it strictly needs full network configuration (like DNS resolution working perfectly before start), use:
# After=network-online.target
# Wants=network-online.target

[Service]
# Type=simple is usually appropriate for servers that run in the foreground
Type=simple

# User and Group to run the service as
User=stevessr
#Group=stevessr # Assumes 'stevessr' is also the primary group name. Check with 'id stevessr' if unsure.

# Working directory where mcpo and config.json reside
WorkingDirectory=/mnt/disk/development/mcpo

# The command to start the service, using the python executable from the venv
# Note: API key is included here directly. Consider using Environment= directive if mcpo supports it.
ExecStart=/mnt/disk/development/mcpo/.venv/bin/mcpo --port 8003 --api-key "sk-stevekey" --config config.json

# Restart the service if it fails
Restart=on-failure
RestartSec=5s # Wait 5 seconds before restarting

# Optional: Define environment variables if needed by the application
# Example: Environment="MY_VAR=some_value"
# Example (more secure for API key if mcpo reads MCPO_API_KEY env var):
# Environment="MCPO_API_KEY=sk-stevekey"
# If using the Environment line above, you would remove --api-key "sk-stevekey" from ExecStart

# Redirect standard output and error to the systemd journal
StandardOutput=journal
StandardError=journal

[Install]
# Enable the service for the default multi-user target (standard system boot)
WantedBy=multi-user.target
```

# [Qwen2API](https://github.com/Rfym21/Qwen2API)
```shell
git clone https://github.com/Rfym21/Qwen2API.git
cd Qwen2API
pnpm i
```

```shell
sudo nano /etc/systemd/system/qwen2api.service
```

```conf
[Unit]
Description=Qwen2API Node.js Application
# Start after the network is available
After=network.target mnt-disk.mount
Wants=mnt-disk.mount

[Service]
# User and Group to run the service as
User=stevessr
#Group=stevessr  # Often the same as the user, adjust if needed

# Working directory for the application
WorkingDirectory=/mnt/disk/development/Qwen2API

# The command to start the application
# IMPORTANT: Using the specific FNM path might be brittle if it changes.
# Consider using a more stable path if FNM provides one (e.g., a symlink)
# or configuring the PATH environment variable below.
ExecStart=/run/user/1000/fnm_multishells/18712_1745111471136/bin/node src/server.js

# Restart the service if it fails
Restart=on-failure
RestartSec=5s

# Optional: Set environment variables (like NODE_ENV)
Environment=NODE_ENV=production
# Add any other required environment variables here, e.g.:
# Environment="PORT=8080"
# Environment="DATABASE_URL=..."

# Redirect stdout/stderr to systemd journal
StandardOutput=journal
StandardError=journal

# Ensure the user's environment (like PATH for node if not using full path above) might be partially loaded
# This might not be strictly necessary if you use the full path to node as above.
# If you wanted to rely on `fnm` setting the path, it gets more complex and might require
# sourcing the user's profile or using fnm's env command within ExecStart.
# For simplicity, the full path in ExecStart is often easiest.

[Install]
# Enable the service for the default multi-user target
WantedBy=multi-user.target
```

# [Veloera/Veloera](https://github.com/Veloera/Veloera)
```shell
wget https://github.com/Veloera/Veloera/releases/latest/download/veloera-linux-arm64
sudo nano /etc/systemd/system/velora.service
```

```conf
[Unit]
Description=Velora Application Service
# If redis/postgres run as systemd services on the same machine, add them here:
After=network.target redis.service postgresql.service mnt-disk.mount
Wants=redis.service postgresql.service mnt-disk.mount

[Service]
# User and Group to run the service as
User=stevessr
#Group=stevessr # Adjust if the group is different

# Working directory for the application
WorkingDirectory=/mnt/disk/development/Veloera

# Command to start the application executable
ExecStart=/mnt/disk/development/Veloera/veloera-linux-arm64

# Restart the service if it fails (equivalent to autorestart: true)
Restart=on-failure
RestartSec=5s

# Environment variables from the 'env' section
Environment="STREAMING_TIMEOUT=6000"
Environment="REDIS_CONN_STRING=redis://localhost:6379/2"
Environment="SQL_DSN=postgres://veloera:veloera@localhost/veloera"
Environment="MEMORY_CACHE_ENABLED=true"
Environment="PORT=8082"
Environment="TIKTOKEN_CACHE_DIR=/mnt/disk/development/Veloera/cache/tiktoken" # Use absolute path

# Redirect stdout/stderr to systemd journal (replaces out_file/error_file)
StandardOutput=journal
StandardError=journal

# Optional: Set a limit on file descriptors if needed
# LimitNOFILE=65536

# Ensure the process receives termination signals correctly
KillSignal=SIGINT
TimeoutStopSec=30

[Install]
# Enable the service for the default multi-user target
WantedBy=multi-user.target
```

# [usememos/memos](https://github.com/usememos/memos)
```shell
sudo nano /etc/systemd/system/memos.service
```

```conf
[Unit]
Description=Memos Application Service
# Start after network and PostgreSQL are available
After=network.target postgresql.service mnt-disk.mount
# Optional: If you want starting memos to also try and start postgresql if it's not running
Wants=postgresql.service mnt-disk.mount

[Service]
# User and Group to run the service as
User=stevessr
#Group=stevessr # Adjust if the group is different

# Working directory for the application
WorkingDirectory=/mnt/disk/development/memos

# Command to start the memos executable
ExecStart=/mnt/disk/development/memos/memos

# Restart the service if it fails (equivalent to autorestart: true)
Restart=on-failure
RestartSec=5s

# Environment variables from the 'env' section
Environment="MEMOS_DRIVER=postgres"
# Note: Ensure your DSN is correct and consider security implications (e.g., file permissions, secrets management)
Environment="MEMOS_DSN=postgresql://memos:memos@localhost:5432/memos?sslmode=disable"
Environment="MEMOS_MODE=prod"
Environment="MEMOS_PORT=5230"
# MEMOS_DATA is set to the working directory, which is common
Environment="MEMOS_DATA=/mnt/disk/development/memos"

# Redirect stdout/stderr to systemd journal (replaces out_file/error_file)
StandardOutput=journal
StandardError=journal

# Ensure the process receives termination signals correctly
KillSignal=SIGINT
TimeoutStopSec=30

[Install]
# Enable the service for the default multi-user target
WantedBy=multi-user.target
```

# [AlistGo/alist: 🗂️](https://github.com/AlistGo/alist)
```shell
wget https://github.com/AlistGo/alist/releases/latest/download/alist-linux-arm64.tar.gz
```

```shell
sudo nano /etc/systemd/system/alist.service
```

```conf
[Unit]
Description=Alist File Server
# Start after the network is available and the filesystem is mounted
After=network.target local-fs.target remote-fs.target postgresql.service mnt-disk.mount
Requires=local-fs.target remote-fs.target postgresql.service mnt-disk.mount

[Service]
# User and Group to run the service as
User=stevessr
#Group=stevessr # Adjust if your group is different

# Type=simple # This is the default, alist server likely runs in foreground

# Working directory for the application
WorkingDirectory=/mnt/disk/development/alist

# The command to start the application (using the full path)
ExecStart=/mnt/disk/development/alist/alist server

# Restart the service if it fails
Restart=on-failure
RestartSec=5s

# Optional: If alist needs specific environment variables, set them here
# Environment="ALIST_CONFIG=/path/to/config.json"
# Environment="ALIST_PORT=5244"

# Redirect stdout/stderr to systemd journal
StandardOutput=journal
StandardError=journal

[Install]
# Enable the service for the default multi-user target
WantedBy=multi-user.target
```

# [Setup - ngrok](https://dashboard.ngrok.com/get-started/setup/linux)
```shell
curl -sSL https://ngrok-agent.s3.amazonaws.com/ngrok.asc \
  | sudo tee /etc/apt/trusted.gpg.d/ngrok.asc >/dev/null \
  && echo "deb https://ngrok-agent.s3.amazonaws.com buster main" \
  | sudo tee /etc/apt/sources.list.d/ngrok.list \
  && sudo apt update \
  && sudo apt install ngrok
```

```shell
sudo nano /etc/systemd/system/ngrok-8001.service
ngrok http --url=working-gnu-badly.ngrok-free.app 8001
```

```conf
[Unit]
Description=Ngrok Tunnel for Port 8001 (working-gnu-badly)
# Start after the network is online
After=network-online.target
Wants=network-online.target
# Optionally, if you have a service for the app on port 8001 (e.g., myapp.service)
# you might want ngrok to start after it:
# After=network-online.target myapp.service
# Wants=myapp.service

[Service]
# User and Group to run the service as
User=stevessr
#Group=stevessr

# Set the home directory explicitly to help ngrok find its config/auth token
Environment=HOME=/home/stevessr  # <-- IMPORTANT: Adjust if stevessr's home is different
# Or, provide the auth token directly (less secure if others can read this file)
# Environment=NGROK_AUTHTOKEN=YOUR_NGROK_AUTH_TOKEN

# The command to start ngrok
# Replace /usr/local/bin/ngrok with the actual path from 'which ngrok'
ExecStart=/usr/local/bin/ngrok http --url=working-gnu-badly.ngrok-free.app 8001 --log=stdout

# Restart the service if it fails (e.g., network issues, ngrok service restart)
Restart=always
RestartSec=10s

# Redirect stdout/stderr to systemd journal
StandardOutput=journal
StandardError=journal

[Install]
# Enable the service for the default multi-user target
WantedBy=multi-user.target
```

# webssh 启动
```shell
mkdir wssh
cd wssh
uv init
uv venv 
source .venv/bin/activate.fish
uv pip install webssh
```

```shell
sudo nano /etc/systemd/system/wssh.service
```

```conf
[Unit]
Description=WSSH Service
# 确保在网络启动后才启动本服务
After=network.target mnt-disk.mount
Wants=mnt-disk.mount

[Service]
# 指定运行服务的用户和组，请根据你的实际情况修改
User=stevessr
#Group=stevessr # 通常与用户名相同，如果你的组名不同，请修改

# 指定命令的工作目录
WorkingDirectory=/mnt/disk/wssh

# 指定要执行的命令，使用虚拟环境中的 wssh 完整路径
ExecStart=/mnt/disk/wssh/.venv/bin/wssh --address='0.0.0.0' --port=8000

# 定义服务失败时的重启策略 (例如: on-failure, always)
Restart=on-failure
# 可以设置重启间隔，例如 5 秒
RestartSec=5

# (可选) 设置环境变量，如果需要的话
# Environment="VAR_NAME=value"

# (可选) 配置标准输出和标准错误日志的记录方式
# StandardOutput=journal # 记录到 systemd journal
# StandardError=journal  # 记录到 systemd journal
# 或者记录到文件:
# StandardOutput=append:/var/log/wssh.log
# StandardError=append:/var/log/wssh_error.log

[Install]
# 定义服务应该在哪个 target 下启用 (通常是多用户环境)
WantedBy=multi-user.target
```

# [glidea/zenfeed:](https://github.com/glidea/zenfeed)

```

```
